{
  "scripts": {
    "emulator:run": "GOOGLE_APPLICATION_CREDENTIALS=../serviceAccountKey.json firebase emulators:start --import db",
    "emulator:export": "firebase emulators:export --force db"
  }
}
