import { ClubLogoDirective } from "./club-logo.directive";

describe("ClubLogoDirective", () => {
  it("should create an instance", () => {
    const directive = new ClubLogoDirective();
    expect(directive).toBeTruthy();
  });
});
