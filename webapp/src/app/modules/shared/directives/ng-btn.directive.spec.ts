import { NgBtnDirective } from "./ng-btn.directive";

describe("NgBtnDirective", () => {
  it("should create an instance", () => {
    const directive = new NgBtnDirective();
    expect(directive).toBeTruthy();
  });
});
